import { Button } from "@/components/ui/button"

export default function SearchButton() {
    return (
        <div>
            <Button className="hover:bg-green-200 active:scale-95 px-4">Search Button</Button>
        </div>
    )
}